# Orbit Fixture Samples

These JSON files simulate the Orbit resources the dashboard consumes:

- `system_time.json`
- `runs.json`
- `run_events.json`
- `run_captures.json`
- `anomalies.json`

Set `ORBIT_USE_FIXTURES=true` to run the app without live internal-network access.
